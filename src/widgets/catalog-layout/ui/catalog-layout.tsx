import { Outlet } from "react-router";

const CatalogLayout = () => {
    return (
        <section>
            Catalog Layout
            <Outlet />
        </section>
    );
};

export default CatalogLayout;
